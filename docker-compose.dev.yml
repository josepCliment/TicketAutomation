services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - .:/var/www/html
    ports:
      - "${APP_PORT:-8080}:80"

volumes:
  sqlite_data:
  storage_data:
